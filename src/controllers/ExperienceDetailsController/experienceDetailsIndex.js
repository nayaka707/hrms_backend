const { addExperienceDetails } = require("./addExperienceDetails");
const { deleteExperienceDetails } = require("./deleteExperienceDetails");

module.exports = {
  addExperienceDetails,
  deleteExperienceDetails
};
